name: api-ci

on:
  push:
    paths:
      - "apps/api/**"
      - "packages/shared/**"
      - ".github/workflows/api.yml"
  pull_request:
    paths:
      - "apps/api/**"
      - "packages/shared/**"

jobs:
  test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm install
      - name: Typecheck
        run: npm run typecheck --workspace @scheduler/api
      - name: Unit tests
        run: npm run test --workspace @scheduler/api
      - name: Build
        run: npm run build --workspace @scheduler/api

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: [test-build]
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy hook
        env:
          RENDER_DEPLOY_HOOK_API: ${{ secrets.RENDER_DEPLOY_HOOK_API }}
        run: |
          if [ -z "$RENDER_DEPLOY_HOOK_API" ]; then
            echo "Render API deploy hook not configured; skipping."
            exit 0
          fi
          curl -fsSL -X POST "$RENDER_DEPLOY_HOOK_API"
